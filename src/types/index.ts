export type UserType = {
  id: number;
  hostUser: boolean;
  isSelected: boolean;
  roomId: string;
  selectedNumberCard: string;
  userName: string;
};

export type ResSelectedNumberCardType = {
  roomId: string;
  selectedNumberCard: string;
  userName: string;
};
